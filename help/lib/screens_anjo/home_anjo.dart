import 'package:flutter/material.dart';

class HomeAnjo extends StatelessWidget {
  const HomeAnjo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
          child: Center(
        child: Text('Home Anjo'),
      )),
    );
  }
}
